#include <bits/stdc++.h>

using namespace std;

#define ll long long int
#define  s string
#define for(i,n) for(int i=0;i<n;i++)
#define dd if(st[i]=='1'&&st[i+1]=='0')
#define cc cnt++
void codechef(int t)
{
    ll n;
    cin>>n;
    s st;
    cin>>st;
    ll cnt=0;
    for(i,n)
    {
        dd
        cc;
    }
    cout<<cnt<<endl;
}

signed main() {
	// your code goes here
	int t;
	cin>>t;
	while(t--)
	{
	    codechef(t);
	}
	return 0;
}
